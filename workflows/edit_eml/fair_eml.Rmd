## FAIR data practices

To make sure our EML has a publisher and entity system associated

Add the following lines to all of your data processing scripts.
```{r, eval= FALSE}
  doc <- eml_add_publisher(doc)
  doc <- eml_add_entity_system(doc)
```

